---
description: A poster shot of a window on a clean backdrop
---

Make a presentable shot of: **$ARGUMENTS**

`shot` with `isolate: true`, a `margin` around 150, and a `backdrop` — `tea-garden` for the
photograph, or `spotlight:<tint>` for a pool of light in a colour that suits what is being shown.
The margin is drawn, not photographed, so no wallpaper, Dock or Stage Manager strip can land in it.

Everything staging moves is put back afterwards. Show them the result.
